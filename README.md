# gestor_kodirepo
Painel gestor para repositorio do kodi

## requesitos:

python 3.9+ instalado na maquina

para windows clicar no arquivo painel.bat para abrir o painel

para linux abrir o terminal e rodar esses comandos:

pip install requests

pip install ttkbootstrap

python main.py
